version: 2.1
jobs:
   scan:
      docker:
          - image: circleci/python:latest
      working_directory: ~/repo
      steps:
          - checkout
          - run: sudo pip install trufflehog
          - run: trufflehog --help
          - run: trufflehog --max_depth 1 --regex ~/repo

workflows:
  scan_image:
    jobs:
      - scan
